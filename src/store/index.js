
// 引入Vue依赖
import Vue from 'vue'
// 引入vuex
import Vuex from 'vuex'

// 使用vuex
Vue.use(Vuex)

// 配置好的仓库导出去
export default new Vuex.Store({
  // 状态 (共享的数据可以定义在这里)
  state: {
  },
  // 数据的修改
  mutations: {
  },
  // 异步的动作
  actions: {
  },
  // 模块
  modules: {
  }
})
